# Trove 工作约定

- 本项目根目录为 `/Users/huijie/Desktop/node/Trove`，所有任务文件和工作路径均位于此目录内。
- 模板：`templates/article-template.md`。
- 文章提取 Skill：`.agents/skills/paper-extractor/SKILL.md`，执行文章提取前读取。
- 原文存放在 `sources/`；临时文件、脚本及页面渲染存放在 `work/`。
- 每篇笔记存放在 `articles/<article-id>/index.md`，其图像放在同目录的 `assets/` 中。
- 知识库不限定研究领域。只提取来源内容，不加入个人评价或推测。
- 未经用户授权，不创建远程仓库、推送或发布内容。
